#include "main.h"
/**
 * _abs - checks for the absolute value of a number
 * @n: number to be checked for absolute value
 *
 * Return: On success 1 || 0 || -1.
 *
 */
int _abs(int n)
{
return abs(n);
}
